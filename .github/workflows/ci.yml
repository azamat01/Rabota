name: CI for Microservices Project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # Job для data_bus
  data_bus:
    runs-on: ubuntu-latest  # Используем актуальную версию Ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Использование последней версии checkout

    - name: Set up Python for data_bus
      uses: actions/setup-python@v3  # Использование последней версии setup-python
      with:
        python-version: '3.8'

    - name: Install dependencies for data_bus
      working-directory: ./microservices_project/data_bus
      run: |
        python -m pip install --upgrade pip
        pip install -e .  # Установка зависимостей из репозитория
        pip install pytest  # Установка pytest

    - name: Run tests for data_bus
      working-directory: ./microservices_project/data_bus
      run: |
        pytest  # Запуск тестов для data_bus

    - name: Verify working directory and current path
      run: |
        echo "Current working directory:"
        pwd  # Печатает текущую директорию
        echo "Listing files in current directory:"
        ls -al  # Печатает список файлов в текущей директории

    - name: Create build directory for data_bus
      run: |
        echo "Creating build directory..."
        mkdir -p ./microservices_project/data_bus/build  # Создаём build директорию
        echo "Creating test file inside build directory..."
        echo "test artifact" > ./microservices_project/data_bus/build/test_file.txt  # Создаём тестовый файл

    - name: Verify build directory contents
      run: |
        echo "Listing contents of build directory:"
        ls -al ./microservices_project/data_bus/build  # Проверяем содержимое build

    - name: Upload build artifacts for data_bus
      uses: actions/upload-artifact@v3  # Использование последней версии actions/upload-artifact v3
      with:
        name: data_bus-artifacts
        path: ./microservices_project/data_bus/build  # Путь к артефактам

  # Аналогичные шаги для order_service и user_service
  order_service:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python for order_service
      uses: actions/setup-python@v3
      with:
        python-version: '3.8'

    - name: Install dependencies for order_service
      working-directory: ./microservices_project/order_service
      run: |
        python -m pip install --upgrade pip
        pip install -e .
        pip install pytest

    - name: Run tests for order_service
      working-directory: ./microservices_project/order_service
      run: |
        pytest

    - name: Create build directory for order_service
      run: |
        echo "Creating build directory..."
        mkdir -p ./microservices_project/order_service/build
        echo "Creating test file inside build directory..."
        echo "test artifact" > ./microservices_project/order_service/build/test_file.txt

    - name: Verify build directory contents
      run: |
        echo "Listing contents of build directory:"
        ls -al ./microservices_project/order_service/build

    - name: Upload build artifacts for order_service
      uses: actions/upload-artifact@v3
      with:
        name: order_service-artifacts
        path: ./microservices_project/order_service/build

  user_service:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python for user_service
      uses: actions/setup-python@v3
      with:
        python-version: '3.8'

    - name: Install dependencies for user_service
      working-directory: ./microservices_project/user_service
      run: |
        python -m pip install --upgrade pip
        pip install -e .
        pip install pytest

    - name: Run tests for user_service
      working-directory: ./microservices_project/user_service
      run: |
        pytest

    - name: Create build directory for user_service
      run: |
        echo "Creating build directory..."
        mkdir -p ./microservices_project/user_service/build
        echo "Creating test file inside build directory..."
        echo "test artifact" > ./microservices_project/user_service/build/test_file.txt

    - name: Verify build directory contents
      run: |
        echo "Listing contents of build directory:"
        ls -al ./microservices_project/user_service/build

    - name: Upload build artifacts for user_service
      uses: actions/upload-artifact@v3
      with:
        name: user_service-artifacts
        path: ./microservices_project/user_service/build
